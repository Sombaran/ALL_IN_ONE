set_defines()
{
	local defines
	local item
	local flags

	defines=${DEFINES}

	for item in ${DEFINES} ; do
		flags="${flags} -D${item}"
	done

	CFLAGS+=" ${flags}"
}

. "`dirname $ac_site_file`/release.defines"

if test -z "$CFLAGS"; then
	CFLAGS="-O2"

	set_defines
fi

