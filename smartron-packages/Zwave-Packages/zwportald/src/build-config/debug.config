set_defines()
{
	local defines
	local item
	local flags

	defines=${DEFINES}

	for item in ${DEFINES} ; do
		flags="${flags} -D${item}"
	done

	CFLAGS+=" ${flags}"
}

. "`dirname $ac_site_file`/debug.defines"

if test -z "$CFLAGS"; then
	CFLAGS="-g -DZWP_DEBUG"

	set_defines
fi

